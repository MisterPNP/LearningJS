
// string challenge in the course
let firstName = "Me";
let lastName = "Not Me";

const fullName = firstName + " " + lastName;
const fullNamev2 = `${firstName} ${lastName}`;

console.log(fullName);
console.log(`${firstName} ${lastName}`); //equivalent to above
console.log(fullNamev2); //equivalent to both above